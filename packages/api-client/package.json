{
    "name": "@askmerve/api-client",
    "version": "0.1.0",
    "description": "Shared HTTP client for AskMerve web and mobile apps",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "test": "echo 'No tests yet'"
    },
    "dependencies": {
        "@askmerve/shared": "workspace:*",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "typescript": "^5.3.0"
    },
    "peerDependencies": {
        "firebase": "^10.0.0"
    }
}